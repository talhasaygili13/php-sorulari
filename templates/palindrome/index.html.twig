<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <title>Palindrom</title>
</head>
<body>
<div >
<h1 class="text-center">Palindrom Kelime Bulucu</h1>
<form class="form-group m-5" method="post">
    <label class="form-label" for="text">Metni getirmek için Metin butonuna tıklayınız. Gelen Metni taramak ve sonuç yazdırmak için Ara butonuna basınızç</label>
    <textarea  class="form-control col-md-8" rows="6" cols="10" name="text" id="text">{{ data | json_encode  }}</textarea>
    <input class="btn btn-secondary btn-lg m-2" type="submit" value="Metin">
    <input class="btn btn-secondary btn-lg m-2"  type="submit" value="Ara">
    <input onclick="checkForm();" class="btn btn-secondary btn-lg m-2" type="reset" value="Sıfırla">
</form>
{% if palindrome|length > 0 %}
    <h2 class="alert alert-success display-6 m-5 text-center "> Toplam kelime sayısı({{ words|length }}), palindrom kelime sayısı({{ palindrome|length }}), Karakter Sayısı({{ len }})</h2>
{% endif %}
{% for palindromes in palindrome %}
    <ul type="none" class="list-group ms-5" >
        <li class="border border-secondary col-md-2 text-center list-group-item">{{ palindromes }}</li>
    </ul>
{% endfor %}
{% if palindrome|length == 0 %} <p class="alert alert-danger display-6 m-5 text-center ">{{ error }}</p> {% endif %}
</div>
<script>
    function checkForm(){
        alert("Kelimeler temizlenecek");
        document.getElementById('text').innerHTML = "";
    }
</script>
</body>
</html>
